package ui;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;

// Handles UI request for connecting to the API and disconnecting from the session
public class ConnectionController {
    @FXML

    public void initialize(){
        //TODO - Get API address to connect to
        // Store login info and token for the session
    }

    @FXML
    /*
    * Sends login request to the API and stores the token
    *
    * */
    private void connectButton(ActionEvent event){

    }
}
